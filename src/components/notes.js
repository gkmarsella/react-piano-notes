import React, { Component } from 'react';

const Note = ({note}) => {
	return (<div>{note}</div>);
}


const NoteList = ({notes}) => {
	const notes = ['A','B','C','D','E','F','G','Ab','Bb','Db','Eb'];
}